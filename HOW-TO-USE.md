# 🎨 Elisa English Hub - 使用說明

## ✨ 恭喜！你的網站已經重新設計完成！

---

## 🎯 主要改進

### 1. **現代化設計**
- ☕ 溫暖的咖啡拿鐵配色（棕色 + 珊瑚橘）
- 🎨 圓角、陰影、微動畫效果
- 📱 完美的響應式設計（手機/平板/電腦）

### 2. **完全自動化**
- 🚀 不需要手動編輯檔案列表
- 📂 只要把 HTML 放進 `intermediate/` 資料夾就自動出現
- 🤖 自動從 HTML 抓取標題、emoji、日期

### 3. **強大搜尋功能**
- 🔍 即時搜尋（打字就顯示結果）
- 📝 搜尋範圍：標題 + 文章內容
- ⚡ 快速、流暢

### 4. **更好的維護性**
- 💯 零維護成本
- ✅ 超簡單的工作流程

---

## 📝 如何新增課程

### 🎯 推薦方式：使用後台管理系統（最簡單！）

#### **步驟 1：開啟後台管理系統**
1. 開啟 `admin.html` 檔案
2. 點擊「新增文章」標籤

#### **步驟 2：填寫文章資訊**
- **課程等級**：選擇 Intermediate 或 Advanced
- **發布日期**：點擊日期選擇器選擇日期
- **文章標題**：輸入標題（含 emoji），例如：`🧠 The Science of Memory`
- **圖片 URL**（選填）：輸入圖片網址，圖片會自動插入到文章 header 之後
- **檔案名稱**：輸入檔名，例如：`The Science of Memory.html`
- **HTML 內容**：貼上 AI 生成的完整 HTML 語法

#### **步驟 3：生成檔案**
1. 點擊「生成檔案」按鈕
2. 複製生成的 HTML 內容
3. 建立新檔案並貼上內容，儲存到對應資料夾：
   - Intermediate → `intermediate/檔名.html`
   - Advanced → `advanced/檔名.html`
4. 複製生成的 `script.js` 更新代碼
5. 打開對應的 `script.js` 或 `script-advanced.js`
6. 找到 `potentialFiles` 陣列，將代碼貼到陣列中

#### **完成！** 🎉
- 重新整理 `index.html` 或 `index-advanced.html` 即可看到新文章
- 自動抓取標題和 emoji
- 自動依照日期排序（最新的在前）
- 自動加入月份分類

---

### 📝 傳統方式：手動新增（進階使用者）

如果你熟悉程式碼，也可以手動新增：

#### **步驟 1：請 AI 生成課程 HTML**
```
你：「幫我生成一個關於 XXX 的英語課程 HTML，
    標題要包含 emoji，專業簡潔，不要紫色不要漸變」

AI：給你完整的 HTML 檔案
```

#### **步驟 2：存檔並放進資料夾**
```
存放位置：
intermediate/你的檔名.html  或  advanced/你的檔名.html

檔名可以自由命名，例如：
✅ Cut Grass.html
✅ AI-Risks.html
✅ bubble-palace.html
```

#### **步驟 3：在 script.js 中註冊新文章**
打開對應的 `script.js`（Intermediate）或 `script-advanced.js`（Advanced），找到 `potentialFiles` 陣列，添加新文章：

```javascript
{ filename: 'The Science of Memory.html', date: '2026-02-06', title: '🧠 The Science of Memory: Why We Remember and Why We Forget' },
// 👆 在此上方添加新文章，記得加逗號！
```

**格式說明：**
- `filename`：檔案名稱（需與實際檔案名稱完全一致）
- `date`：發布日期（格式：`YYYY-MM-DD`）
- `title`：文章標題（含 emoji，如果標題包含單引號，需使用 `\'` 轉義）

---

## 🎨 設計特色

### **顏色主題**
```css
主色：#8b6f47  (咖啡棕 - 溫暖專業)
輔色：#c9a86a  (拿鐵奶茶 - 柔和舒適)
強調：#d97757  (珊瑚橘 - 活潑明亮)
背景：#faf8f5  (米白色 - 乾淨舒適)
```

### **視覺元素**
- 📦 卡片式設計，輕微陰影
- 🎯 Hover 效果：微上浮 + 左移
- 📅 日期顯示：低調但清楚
- 🖼️ 文章圖片：可在 header 之後顯示相關圖片

---

## 🔍 搜尋功能說明

### **搜尋範圍**
- ✅ 標題（如：「Bubble Palace」）
- ✅ Emoji（如：「🏰」）
- ✅ 文章內容前 500 字（如：「architect」）

### **使用方式**
1. 在側邊欄搜尋框打字
2. 即時顯示搜尋結果
3. 清空搜尋框 = 顯示全部課程

---

## 📱 響應式設計

### **前台頁面（index.html / index-advanced.html）**

#### **桌面版（> 768px）**
```
┌─────────────────────────────────────┐
│ Header                              │
│ • 標題 + 等級標籤                    │
│ • 搜尋框（置中）                     │
├─────────────────────────────────────┤
│ Main Content                        │
│ • Welcome Card                      │
│ • 月曆視圖（可展開/收合）            │
│   - 月份標題 + 課程數量              │
│   - 月曆表格（週一到週日）           │
│   - 課程卡片（顯示在對應日期）       │
└─────────────────────────────────────┘
```

#### **平板版（768px - 1024px）**
- Header 保持完整顯示
- 搜尋框寬度自動調整
- 月曆表格字體稍小，但保持完整結構
- 課程卡片保持可讀性

#### **手機版（< 768px）**
```
┌─────────────────────┐
│ Header              │
│ • 標題（較小字體）   │
│ • 等級標籤           │
│ • 搜尋框（全寬）     │
├─────────────────────┤
│ Main Content        │
│ • Welcome Card      │
│ • 月曆視圖          │
│   - 月份標題        │
│   - 月曆表格        │
│     （較小字體）    │
│   - 課程卡片        │
│     （單列顯示）    │
└─────────────────────┘
```

**手機版特色：**
- 月曆表格字體縮小以適應螢幕
- 課程卡片自動換行
- 所有互動元素觸控友善（按鈕大小 ≥ 44px）
- 滾動流暢，無水平滾動條

---

### **後台管理系統（admin.html）**

#### **桌面版（> 768px）**
```
┌─────────────────────────────────────┐
│ Header                              │
│ • 標題 + 說明                        │
├─────────────────────────────────────┤
│ Tabs                                │
│ [新增文章] [編輯文章]                │
├─────────────────────────────────────┤
│ Content                             │
│ • 表單（兩欄並排）                   │
│   - 左：課程等級                     │
│   - 右：發布日期                     │
│ • 單欄輸入框                         │
│   - 標題、圖片 URL、檔名、HTML        │
│ • 按鈕群組                           │
│ • 輸出區域（代碼顯示）               │
└─────────────────────────────────────┘
```

#### **平板版（768px - 1024px）**
- 表單保持兩欄布局
- 所有輸入框保持可讀性
- 按鈕大小適中

#### **手機版（< 768px）**
```
┌─────────────────────┐
│ Header              │
│ • 標題（較小）       │
├─────────────────────┤
│ Tabs                │
│ [新增] [編輯]       │
├─────────────────────┤
│ Content             │
│ • 表單（單欄）       │
│   - 所有欄位垂直排列│
│ • 按鈕（全寬）       │
│ • 輸出區域           │
│   （可橫向滾動）     │
└─────────────────────┘
```

**手機版特色：**
- 表單欄位改為單欄垂直排列
- 按鈕改為全寬顯示，方便點擊
- 代碼輸出區域支援橫向滾動
- 圖片預覽自動縮放適應螢幕

---

### **文章頁面（課程 HTML）**

#### **桌面版（> 768px）**
- 內容區域最大寬度約 1200px
- 圖片完整顯示
- 文字行寬適中，易於閱讀

#### **手機版（< 768px）**
- 內容自動適應螢幕寬度
- 圖片自動縮放
- 文字大小保持可讀性
- 表格和列表自動換行

---

## 🛠️ 技術說明

### **檔案結構**
```
elisa-english-hub/
├── index.html              (Intermediate 首頁)
├── index-advanced.html     (Advanced 首頁)
├── admin.html              (後台管理系統) ⭐ 新增
├── style-intermediate.css   (Intermediate 樣式表)
├── style-advanced.css      (Advanced 樣式表)
├── script.js               (Intermediate 功能腳本)
├── script-advanced.js      (Advanced 功能腳本)
├── HOW-TO-USE.md          (本說明文件)
├── intermediate/           (Intermediate 課程資料夾)
│   ├── The Science of Memory.html
│   ├── AI-Risks.html
│   └── ...
└── advanced/               (Advanced 課程資料夾)
    ├── Hollywood Means Business.html
    ├── The New Primetime.html
    └── ...
```

### **文章載入機制**
1. 頁面載入時，JavaScript 讀取 `script.js` 中 `potentialFiles` 陣列列出的檔案
2. 讀取每個 HTML 檔案
3. 抓取 `<h1>` 作為標題（包含 emoji）
4. 解析日期（優先順序）：
   - 手動指定的日期（物件格式中的 `date`）
   - 從檔名解析（格式：`YYYY-MM-DD-topic.html`）
   - 從 HTML meta 標籤提取（如果有的話）
   - 使用當前日期（fallback）
5. 抓取前 500 字作為搜尋內容
6. 依照日期排序（最新的在前）
7. 自動建立月份分類

---

## 🚨 重要提醒

### **檔名規則**

檔名可以自由命名，但建議使用以下格式：

**推薦格式 1：包含日期（方便排序）**
```
✅ 2025-01-10-bubble-palace.html
✅ 2025-12-25-christmas-lesson.html
✅ 2026-03-15-spring-vocabulary.html
```
說明：系統會自動從檔名解析日期，無需手動指定

**推薦格式 2：不包含日期（需在 script.js 中指定）**
```
✅ Cut Grass.html
✅ AI-Risks.html
✅ bubble-palace.html
```
說明：需要在 `script.js` 中使用物件格式指定日期：
```javascript
{ filename: 'Cut Grass.html', date: '2025-01-20' }
```

**日期格式說明：**
- 必須使用 `YYYY-MM-DD` 格式（年-月-日）
- 範例：`2025-01-20`、`2025-12-25`

### **HTML 檔案要求**
你的 AI 生成的 HTML **必須包含**：
- `<h1>` 標籤（建議包含 emoji）
- 完整的 `<style>` 標籤（AI 自由發揮）
- 正常的 HTML 結構

範例：
```html
<!DOCTYPE html>
<html lang="en">
<head>
    <title>Lesson Title</title>
    <style>
        /* AI 生成的樣式 */
    </style>
</head>
<body>
    <h1>🏰 The Bubble Palace</h1>
    <!-- 課程內容 -->
</body>
</html>
```

---

## 🎁 額外功能

### **後台管理系統** ⭐ 新功能
- 📝 **新增文章**：透過表單輕鬆新增課程
- ✏️ **編輯文章**：查看所有文章列表，點擊編輯即可修改
- 🖼️ **圖片管理**：可為每篇文章添加圖片 URL
- 📋 **自動生成**：自動生成 HTML 檔案和 script.js 更新代碼
- 👀 **預覽功能**：在生成前預覽文章效果

**使用方式：**
1. 開啟 `admin.html`
2. 選擇「新增文章」或「編輯文章」標籤
3. 填寫表單並生成檔案
4. 複製代碼並更新對應檔案

### **回到頂部按鈕**
- 滾動超過 300px 自動出現
- 點擊平滑滾動到頂部
- 浮動在右下角

### **Debug 功能**
在瀏覽器 Console 輸入：
```javascript
debugLessons()
```
會顯示所有課程的資料表格，方便檢查。

---

## 🌟 功能清單

### **已完成功能** ✅
- ✅ 後台管理系統（新增/編輯文章）
- ✅ 圖片 URL 支援（可在文章 header 後顯示圖片）
- ✅ 依照日期自動排序
- ✅ 基本搜尋（標題 + 內容前 500 字）
- ✅ 月份分類
- ✅ 響應式設計（手機/平板/電腦）

### **未來擴充建議** 🚀
- 🏷️ 主題分類（Food, Business, Culture...）
- 💾 收藏功能（學生可以收藏想上的課）
- 📊 統計功能（每月課程數量圖表）
- 🔍 進階搜尋（全文搜尋、難度篩選）
- 🔗 GitHub API 整合（自動上傳檔案）

---

## 📞 需要幫助？

### **常見問題**

**Q: 如何使用後台管理系統？**
A: 
1. 開啟 `admin.html` 檔案
2. 點擊「新增文章」標籤
3. 填寫表單（等級、日期、標題、HTML 內容等）
4. 點擊「生成檔案」
5. 複製生成的代碼並更新對應檔案

**Q: 如何編輯現有文章？**
A:
1. 開啟 `admin.html`
2. 點擊「編輯文章」標籤
3. 選擇課程等級（Intermediate 或 Advanced）
4. 點擊文章右側的「編輯」按鈕
5. 修改內容後重新生成檔案

**Q: 如何添加文章圖片？**
A:
1. 在後台管理系統的「圖片 URL」欄位輸入圖片網址
2. 圖片會自動插入到文章 header 之後
3. 可以即時預覽圖片效果

**Q: 新增的課程沒有出現？**
A: 確認以下幾點：
   1. HTML 檔案已放在正確的資料夾（`intermediate/` 或 `advanced/`）
   2. 已在對應的 `script.js` 或 `script-advanced.js` 的 `potentialFiles` 陣列中添加檔案資訊
   3. 檔案名稱與 script.js 中的檔名完全一致
   4. 重新整理頁面（按 F5 或 Ctrl+R）

**Q: 如何修改文章的發布日期？**
A:
1. 使用後台管理系統的「編輯文章」功能
2. 選擇要編輯的文章
3. 修改日期欄位
4. 重新生成檔案並更新 script.js

**Q: 搜尋找不到某個單字？**
A: 目前只搜尋前 500 字，如果需要全文搜尋請告訴我

**Q: 可以改顏色嗎？**
A: 可以！在 `style-intermediate.css` 或 `style-advanced.css` 的 `:root` 區塊改 CSS 變數

**Q: 手機版顯示怪怪的？**
A: 清除瀏覽器快取後重新載入

**Q: 後台管理系統無法載入文章列表？**
A: 
- 確認 `admin.html` 與 `script.js`、`script-advanced.js` 在同一資料夾
- 某些瀏覽器可能阻擋本地檔案讀取，建議使用本地伺服器（如 VS Code 的 Live Server）

---

## 🎉 開始使用吧！

1. 開啟 `index.html`
2. 看看新設計的效果
3. 試試搜尋功能
4. 點幾個課程看看

**享受你的新網站！** ☕✨

---

---

最後更新：2026-02-06
設計者：Claude with ❤️

## 📚 更新日誌

### 2026-02-06
- ✨ 新增後台管理系統（`admin.html`）
- ✨ 新增圖片 URL 功能
- 🗑️ 移除 NEW 標籤功能
- 📝 更新使用說明文件
