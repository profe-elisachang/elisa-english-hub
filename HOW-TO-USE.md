# 🎨 Elisa English Hub - 使用說明

## ✨ 恭喜！你的網站已經重新設計完成！

---

## 🎯 主要改進

### 1. **現代化設計**
- ☕ 溫暖的咖啡拿鐵配色（棕色 + 珊瑚橘）
- 🎨 圓角、陰影、微動畫效果
- 📱 完美的響應式設計（手機/平板/電腦）

### 2. **完全自動化**
- 🚀 不需要手動編輯檔案列表
- 📂 只要把 HTML 放進 `intermediate/` 資料夾就自動出現
- 🤖 自動從 HTML 抓取標題、emoji、日期

### 3. **強大搜尋功能**
- 🔍 即時搜尋（打字就顯示結果）
- 📝 搜尋範圍：標題 + 文章內容
- ⚡ 快速、流暢

### 4. **更好的維護性**
- 💯 零維護成本
- ✅ 超簡單的工作流程

---

## 📝 如何新增課程

### 超簡單！只要 3 步驟：

#### **步驟 1：請 AI 生成課程 HTML**
```
你：「幫我生成一個關於 XXX 的英語課程 HTML，
    標題要包含 emoji，專業簡潔，不要紫色不要漸變」

AI：給你完整的 HTML 檔案
```

#### **步驟 2：存檔並放進資料夾**
```
存放位置：
intermediate/你的檔名.html

檔名可以自由命名，例如：
✅ Cut Grass.html
✅ AI-Risks.html
✅ bubble-palace.html
```

#### **步驟 3：在 script.js 中註冊新文章**
打開 `script.js`，找到 `potentialFiles` 陣列，添加新文章：

**方式 1：檔名包含日期（推薦）**
```javascript
'2025-01-15-bubble-palace.html',  // 系統會自動從檔名解析日期
```

**方式 2：檔名不包含日期（需手動指定日期）**
```javascript
{ filename: 'Cut Grass.html', date: '2025-01-20' },
```

**完整範例：**
```javascript
const potentialFiles = [
    '2025-01-15-bubble-palace.html',              // 方式 1
    { filename: 'Cut Grass.html', date: '2025-01-20' },  // 方式 2
    // 👆 在此上方添加新文章，記得加逗號！
];
```

#### **完成！** 🎉
- 重新整理 `index.html` 即可看到新文章
- 自動抓取標題和 emoji
- 自動依照日期排序（最新的在前）
- 自動加入月份分類
- 自動計算「NEW」標籤（7 天內的文章）

---

## 🎨 設計特色

### **顏色主題**
```css
主色：#8b6f47  (咖啡棕 - 溫暖專業)
輔色：#c9a86a  (拿鐵奶茶 - 柔和舒適)
強調：#d97757  (珊瑚橘 - 活潑明亮)
背景：#faf8f5  (米白色 - 乾淨舒適)
```

### **視覺元素**
- 📦 卡片式設計，輕微陰影
- 🎯 Hover 效果：微上浮 + 左移
- 🏷️ NEW 標籤：7 天內的課程自動標示
- 📅 日期顯示：低調但清楚

---

## 🔍 搜尋功能說明

### **搜尋範圍**
- ✅ 標題（如：「Bubble Palace」）
- ✅ Emoji（如：「🏰」）
- ✅ 文章內容前 500 字（如：「architect」）

### **使用方式**
1. 在側邊欄搜尋框打字
2. 即時顯示搜尋結果
3. 清空搜尋框 = 顯示全部課程

---

## 📱 響應式設計

### **桌面版（> 768px）**
```
┌─────────────────────────────────────┐
│ Header (Logo + Badge)               │
├──────────┬──────────────────────────┤
│ Sidebar  │  Main Content            │
│ (固定)   │  (可滾動)                 │
│          │                          │
│ • 搜尋   │  • Welcome Card          │
│ • 月份   │  • Lesson List           │
└──────────┴──────────────────────────┘
```

### **手機版（< 768px）**
```
┌─────────────────────┐
│ Header              │
├─────────────────────┤
│ Sidebar (上方)      │
├─────────────────────┤
│ Main Content        │
│ (單列顯示)          │
└─────────────────────┘
```

---

## 🛠️ 技術說明

### **檔案結構**
```
elisa-english-hub/
├── index.html              (首頁)
├── style-intermediate.css  (樣式表 - 全新設計)
├── script.js               (功能腳本 - 自動掃描)
├── HOW-TO-USE.md          (本說明文件)
└── intermediate/
    ├── 2025-01-10-bubble-palace.html
    ├── 2025-01-12-yakult-secret.html
    └── 2025-01-15-enthusiasm.html
```

### **文章載入機制**
1. 頁面載入時，JavaScript 讀取 `script.js` 中 `potentialFiles` 陣列列出的檔案
2. 讀取每個 HTML 檔案
3. 抓取 `<h1>` 作為標題（包含 emoji）
4. 解析日期（優先順序）：
   - 手動指定的日期（物件格式中的 `date`）
   - 從檔名解析（格式：`YYYY-MM-DD-topic.html`）
   - 從 HTML meta 標籤提取（如果有的話）
   - 使用當前日期（fallback）
5. 抓取前 500 字作為搜尋內容
6. 依照日期排序（最新的在前）
7. 自動建立月份分類

---

## 🚨 重要提醒

### **檔名規則**

檔名可以自由命名，但建議使用以下格式：

**推薦格式 1：包含日期（方便排序）**
```
✅ 2025-01-10-bubble-palace.html
✅ 2025-12-25-christmas-lesson.html
✅ 2026-03-15-spring-vocabulary.html
```
說明：系統會自動從檔名解析日期，無需手動指定

**推薦格式 2：不包含日期（需在 script.js 中指定）**
```
✅ Cut Grass.html
✅ AI-Risks.html
✅ bubble-palace.html
```
說明：需要在 `script.js` 中使用物件格式指定日期：
```javascript
{ filename: 'Cut Grass.html', date: '2025-01-20' }
```

**日期格式說明：**
- 必須使用 `YYYY-MM-DD` 格式（年-月-日）
- 範例：`2025-01-20`、`2025-12-25`

### **HTML 檔案要求**
你的 AI 生成的 HTML **必須包含**：
- `<h1>` 標籤（建議包含 emoji）
- 完整的 `<style>` 標籤（AI 自由發揮）
- 正常的 HTML 結構

範例：
```html
<!DOCTYPE html>
<html lang="en">
<head>
    <title>Lesson Title</title>
    <style>
        /* AI 生成的樣式 */
    </style>
</head>
<body>
    <h1>🏰 The Bubble Palace</h1>
    <!-- 課程內容 -->
</body>
</html>
```

---

## 🎁 額外功能

### **回到頂部按鈕**
- 滾動超過 300px 自動出現
- 點擊平滑滾動到頂部
- 浮動在右下角

### **NEW 標籤**
- 自動標示 7 天內的新課程
- 珊瑚橘背景，白色文字
- 可在 `script.js` 調整天數：
  ```javascript
  const CONFIG = {
      newLessonDays: 7  // 改成你想要的天數
  };
  ```

### **Debug 功能**
在瀏覽器 Console 輸入：
```javascript
debugLessons()
```
會顯示所有課程的資料表格，方便檢查。

---

## 🌟 未來擴充建議

### **階段 1（現在）**
✅ 手動註冊檔案（在 script.js 中）
✅ 依照日期自動排序
✅ 基本搜尋（標題 + 內容前 500 字）
✅ 月份分類
✅ NEW 標籤

### **階段 2（未來可選）**
- 🏷️ 主題分類（Food, Business, Culture...）
- 💾 收藏功能（學生可以收藏想上的課）
- 📊 統計功能（每月課程數量圖表）
- 🔍 進階搜尋（全文搜尋、難度篩選）

---

## 📞 需要幫助？

### **常見問題**

**Q: 新增的課程沒有出現？**
A: 確認以下兩點：
   1. 檔案已放在 `intermediate/` 資料夾
   2. 已在 `script.js` 的 `potentialFiles` 陣列中添加檔案名稱
   3. 重新整理頁面（按 F5 或 Ctrl+R）

**Q: 搜尋找不到某個單字？**
A: 目前只搜尋前 500 字，如果需要全文搜尋請告訴我

**Q: 可以改顏色嗎？**
A: 可以！在 `style-intermediate.css` 的 `:root` 區塊改 CSS 變數

**Q: 手機版顯示怪怪的？**
A: 清除瀏覽器快取後重新載入

---

## 🎉 開始使用吧！

1. 開啟 `index.html`
2. 看看新設計的效果
3. 試試搜尋功能
4. 點幾個課程看看

**享受你的新網站！** ☕✨

---

最後更新：2025-01-12
設計者：Claude with ❤️
