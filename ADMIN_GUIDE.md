# 📝 管理系統使用指南 v2.2

## 🎉 最新版本功能

我們整合了兩個版本的優點，創建了最佳的管理體驗！

### ✅ 核心功能

1. **月曆視圖主頁**
   - 一目瞭然看到每月的文章安排
   - 點擊文章標題直接進入編輯模式
   - 即時搜尋功能

2. **分屏即時預覽編輯器**
   - 左側 50%：編輯區
   - 右側 50%：即時預覽區
   - 可拖拉的分隔線調整比例
   - 全屏工作環境，不是彈窗！

3. **雙編輯模式**
   - **HTML 編輯模式**：直接編輯程式碼
   - **視覺化編輯模式**：所見即所得
   - 一鍵切換

4. **明確的操作模式**
   - 新增模式（綠色標籤）
   - 更新模式（黃色標籤）
   - 清楚顯示當前操作

5. **草稿功能**
   - 隨時儲存草稿（本地瀏覽器）
   - 自動提示載入未完成的草稿
   - 支援視覺化模式的草稿

6. **GitHub 整合 ⭐ NEW**
   - 點擊「✅ 發布文章」或「✅ 儲存更新」自動上傳到 GitHub
   - 自動更新 `script.js` 或 `script-advanced.js`
   - 支援檔案重新命名（自動刪除舊檔案）
   - 所有變更都有完整的 commit 記錄

7. **獨立管理頁面**
   - `admin-intermediate.html` - Intermediate 專用
   - `admin-advanced.html` - Advanced 專用

---

## 📖 詳細使用說明

### ⚙️ 首次設定（GitHub 整合）

**重要：**在第一次使用儲存功能前，需要設定 GitHub 認證資訊。

#### 步驟 1：取得 GitHub Personal Access Token

1. 登入 GitHub
2. 前往：Settings → Developer settings → Personal access tokens → Tokens (classic)
3. 點擊「Generate new token (classic)」
4. 設定以下權限：
   - `repo`（完整存取權限）
5. 複製產生的 Token（**只會顯示一次！**）

#### 步驟 2：設定到瀏覽器

開啟瀏覽器開發者工具 Console（按 F12），執行：

```javascript
// 設定 GitHub Token
localStorage.setItem('github_token', 'ghp_your_token_here');

// 設定 GitHub 使用者名稱
localStorage.setItem('github_username', 'your_username');

// 設定 GitHub Repository 名稱
localStorage.setItem('github_repo', 'your_repo_name');
```

**範例：**
```javascript
localStorage.setItem('github_token', 'ghp_1234567890abcdefghijklmnopqrstuvwxyz');
localStorage.setItem('github_username', 'elisa');
localStorage.setItem('github_repo', 'elisa-english-hub');
```

#### 驗證設定

儲存後，重新整理頁面，然後嘗試儲存一篇文章。如果出現以下訊息表示設定成功：
- ✅ 「文章已成功儲存！」
- ✅ 「HTML 檔案已更新」
- ✅ 「script.js 已更新」

如果出現錯誤訊息，請檢查：
- Token 是否正確
- 使用者名稱和 Repository 名稱是否正確
- Token 權限是否包含 `repo`

---

### 開啟管理頁面

**方式 1：直接開啟**
- Intermediate：開啟 `admin-intermediate.html`
- Advanced：開啟 `admin-advanced.html`

**方式 2：從頭部導航**
- 點擊「Intermediate 管理」或「Advanced 管理」按鈕

### 新增文章

1. 點擊右上角「➕ 新增文章」
2. 頁面切換到**分屏編輯器**
3. 看到綠色「新增模式」標籤
4. 左側填寫表單：
   - 文章標題（含 emoji）
   - 發布日期
   - 檔案名稱
   - HTML 內容
5. **右側即時看到預覽效果**
6. 可隨時點擊「💾 儲存草稿」
7. 完成後點擊「✅ 發布文章」

### 編輯文章

1. 在月曆視圖找到要編輯的文章
2. 點擊文章標題
3. 頁面切換到**分屏編輯器**
4. 看到黃色「更新模式」標籤
5. 左側編輯內容，右側即時預覽
6. 完成後點擊「✅ 儲存更新」

### 搜尋文章

1. 在主頁面搜尋框輸入關鍵字
2. 月曆即時過濾
3. 清空搜尋框回到完整列表

---

## 🎨 分屏編輯器功能

### 調整編輯區/預覽區比例

- 將滑鼠移到中間的分隔線
- 按住拖拉調整左右比例
- 比例會自動儲存（下次開啟維持相同比例）

### 切換編輯模式

**HTML 編輯模式：**
- 適合熟悉 HTML 的使用者
- 直接編輯程式碼
- 使用 monospace 字體

**視覺化編輯模式：**
- 所見即所得
- 像 Word 一樣編輯
- 適合快速調整內容

**切換方式：**
- 點擊「👁️ 視覺化編輯」或「📝 HTML 編輯」按鈕

### 預覽功能

**自動預覽：**
- 輸入時自動更新預覽
- 狀態欄顯示最後更新時間

**手動重新整理：**
- 點擊「🔄 重新整理」按鈕

**全螢幕預覽：**
- 點擊「⛶ 全螢幕」按鈕
- 按 ESC 退出全螢幕

---

## 💾 草稿功能 vs 正式儲存

### 💡 兩種儲存方式的差異

| 功能 | 💾 儲存草稿 | ✅ 發布文章/儲存更新 |
|------|-------------|---------------------|
| 儲存位置 | 本地瀏覽器 | GitHub Repository |
| 持久性 | 清除瀏覽器資料會遺失 | 永久保存 |
| 可見性 | 僅自己看得到 | 所有人可見（發布到網站） |
| 用途 | 臨時儲存、防止意外遺失 | 正式發布或更新文章 |
| 速度 | 瞬間完成 | 需要網路連線 |

### 儲存草稿（本地）

1. 編輯過程中點擊「💾 儲存草稿」
2. 右下角顯示「草稿已自動儲存」提示
3. 草稿儲存在瀏覽器本地（LocalStorage）
4. **用於防止意外關閉頁面導致內容遺失**

### 載入草稿

1. 下次開啟管理頁面時
2. 系統自動偵測到草稿
3. 詢問「找到草稿，要載入嗎？」
4. 點擊確認載入
5. **會還原編輯模式**（HTML 或視覺化）

### 正式儲存（GitHub）

1. 完成編輯後點擊「✅ 發布文章」或「✅ 儲存更新」
2. 系統執行以下動作：
   - 上傳 HTML 檔案到 GitHub（`intermediate/` 或 `advanced/` 資料夾）
   - 更新 `script.js` 或 `script-advanced.js`（添加或更新文章記錄）
   - 如果檔名改變，自動刪除舊檔案
   - 產生 Git commit 記錄
3. 顯示「✅ 文章已成功儲存！」
4. 自動關閉編輯器並重新載入文章列表

### 注意事項

**草稿：**
- 一個等級只能有一份草稿
- 新草稿會覆蓋舊的
- 清除瀏覽器資料會刪除草稿
- 正式儲存後，草稿不會自動刪除（需手動清除）

**正式儲存：**
- 需要先設定 GitHub Token（見上方「首次設定」）
- 需要網路連線
- 會立即發布到網站
- 所有變更都有完整的 Git 歷史記錄

---

## ⌨️ 快捷操作

| 操作 | 方法 |
|------|------|
| 新增文章 | 點擊「➕ 新增文章」 |
| 編輯文章 | 點擊月曆中的文章標題 |
| 搜尋文章 | 在搜尋框輸入關鍵字 |
| 切換編輯模式 | 點擊編輯器中的按鈕 |
| 調整分屏比例 | 拖拉中間分隔線 |
| 重新整理預覽 | 點擊「🔄 重新整理」 |
| 全螢幕預覽 | 點擊「⛶ 全螢幕」 |
| 儲存草稿 | 點擊「💾 儲存草稿」 |
| 關閉編輯器 | 點擊「✕ 關閉」或右上角按鈕 |

---

## 🚀 工作流程建議

### 新增文章的最佳流程

1. 點擊「➕ 新增文章」
2. 填寫標題、日期、檔名
3. 切換到「視覺化編輯模式」
4. 貼上或輸入內容
5. 即時在右側查看效果
6. 調整格式和排版
7. 如需修改 HTML，切換到「HTML 編輯模式」
8. 定期點擊「💾 儲存草稿」（避免意外遺失）
9. 確認無誤後點擊「✅ 發布文章」

### 編輯文章的最佳流程

1. 在月曆中找到文章並點擊
2. 查看右側預覽，確認當前內容
3. 在左側進行修改
4. 即時看到右側的變化
5. 使用「🔄 重新整理預覽」確認效果
6. 滿意後點擊「✅ 儲存更新」

---

## 💡 進階技巧

### 調整最佳的編輯/預覽比例

**編輯為主：**
- 將分隔線拖到右側（70% / 30%）
- 適合大量輸入內容時

**預覽為主：**
- 將分隔線拖到左側（30% / 70%）
- 適合檢查排版效果時

**平衡模式：**
- 保持 50% / 50%（預設）
- 適合同時編輯和檢查

### 避免內容遺失

1. **長文章編輯**：每 15 分鐘點擊「儲存草稿」
2. **重要修改前**：先複製內容到記事本
3. **測試新功能時**：使用草稿功能
4. **網路不穩時**：頻繁儲存草稿

### 快速檢查格式

1. 在視覺化模式下貼上內容
2. 切換到全螢幕預覽
3. 滾動查看整篇文章
4. 發現問題後按 ESC 退出
5. 直接在左側修改

---

## 🔧 故障排除

### 儲存失敗：「請先設定 GitHub Token」

**原因：**未設定 GitHub 認證資訊

**解決方法：**
1. 按 F12 開啟開發者工具
2. 切換到 Console 分頁
3. 執行以下指令（替換成您的資訊）：
```javascript
localStorage.setItem('github_token', 'ghp_your_token_here');
localStorage.setItem('github_username', 'your_username');
localStorage.setItem('github_repo', 'your_repo_name');
```
4. 重新整理頁面

### 儲存失敗：「上傳檔案錯誤」

**可能原因：**
1. Token 已過期或權限不足
2. Repository 名稱錯誤
3. 網路連線問題

**解決方法：**
1. 檢查 GitHub Token 是否有效（到 GitHub Settings 確認）
2. 確認 Token 權限包含 `repo`
3. 檢查網路連線
4. 在 Console 查看詳細錯誤訊息

### 文章列表顯示不完整

**原因：**`script.js` 或 `script-advanced.js` 格式錯誤

**解決方法：**
1. 按 F12 開啟開發者工具
2. 查看 Console 是否有錯誤訊息
3. 檢查對應的 script 檔案
4. 確認 `potentialFiles` 陣列格式正確

### 月曆日期顯示錯誤（早一天）

**原因：**時區問題（已修正）

**確認修正：**
- 檢查文章顯示日期是否正確
- 如果仍有問題，請回報

### 預覽內容與實際不符

**解決方法：**
1. 點擊「🔄 重新整理預覽」
2. 如果仍不符，切換編輯模式（HTML ↔ 視覺化）
3. 檢查 HTML 語法是否正確

### 草稿無法載入

**可能原因：**
1. 草稿資料損壞
2. 瀏覽器 LocalStorage 已滿

**解決方法：**
1. 清除舊草稿：
```javascript
// Intermediate
localStorage.removeItem('admin_draft_intermediate');
// Advanced
localStorage.removeItem('admin_draft_advanced');
```
2. 清除瀏覽器快取後重試

### 如何查看 Git Commit 歷史

在 GitHub 網站上：
1. 進入您的 Repository
2. 點擊上方「commits」連結
3. 查看所有儲存記錄
4. 點擊特定 commit 查看詳細變更

---

## 📊 檢查清單

### 首次使用前

- [ ] 已取得 GitHub Personal Access Token
- [ ] Token 權限包含 `repo`
- [ ] 已在 Console 設定 `github_token`
- [ ] 已在 Console 設定 `github_username`
- [ ] 已在 Console 設定 `github_repo`
- [ ] 測試儲存功能是否正常

### 發布文章前

- [ ] 標題完整且正確
- [ ] 日期正確
- [ ] 檔案名稱符合命名規則
- [ ] 檢查右側預覽效果
- [ ] 使用「🔄 重新整理預覽」再次確認
- [ ] 已儲存草稿（防止意外）

### 發布文章後

- [ ] 確認成功訊息「✅ 文章已成功儲存！」
- [ ] 月曆上出現新文章
- [ ] 到 GitHub 確認 commit 記錄
- [ ] 到前台網站確認文章顯示正常
- [ ] 清除草稿（可選）

---

## 🆘 常見問題 Q&A

**Q: 草稿和正式儲存有什麼差別？**  
A: 草稿儲存在本地瀏覽器，僅供暫存；正式儲存會上傳到 GitHub，發布到網站。

**Q: 可以不設定 GitHub Token 嗎？**  
A: 不行。沒有 Token 無法上傳檔案到 GitHub。

**Q: Token 會過期嗎？**  
A: 會。建議設定「No expiration」或定期更新。

**Q: 可以在手機上使用嗎？**  
A: 技術上可以，但建議使用電腦以獲得最佳體驗（尤其是分屏編輯器）。

**Q: 多人可以同時編輯嗎？**  
A: 不建議。可能造成 Git 衝突。建議協調編輯時間。

**Q: 如何查看舊版本文章？**  
A: 到 GitHub Repository → 找到檔案 → 點擊 "History" → 選擇特定 commit。

**Q: 誤刪文章怎麼辦？**  
A: 到 GitHub 查看 commit 歷史，找到刪除前的版本並復原。

**Q: 儲存失敗會怎樣？**  
A: 顯示錯誤訊息，內容不會遺失（仍在編輯器中），可儲存草稿後再試。

---

## 📝 更新記錄

### v2.2 (2026-02-16)
- ✅ 整合 GitHub API 上傳功能
- ✅ 修正「💾 儲存草稿」按鈕樣式（文字可見性問題）
- ✅ 修正月曆日期顯示早一天的問題（時區）
- ✅ 完善錯誤處理和提示訊息
- ✅ 新增詳細的 GitHub 設定說明

### v2.1 (2026-02-15)
- ✅ 整合分屏即時預覽編輯器
- ✅ 實作月曆視圖
- ✅ 獨立 Intermediate 和 Advanced 管理頁面
- ✅ 新增草稿功能
- ✅ 明確區分「新增」vs「更新」模式

### v2.0
- 初始版本

---

## 💬 回饋與支援

如有問題或建議，請：
1. 檢查本文檔的「故障排除」章節
2. 查看瀏覽器 Console 的錯誤訊息
3. 確認 GitHub Token 設定正確
4. 聯繫開發者
